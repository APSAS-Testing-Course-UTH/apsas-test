product: jvm/app
apply:
  - ../spring-cloud.module-template.yaml

settings:
  jvm:
    mainClass: apsas.configserver.ConfigServerApplication

dependencies:
  - $libs.spring.cloud.config.server
  - $libs.spring.cloud.starter.netflix.eureka.client
