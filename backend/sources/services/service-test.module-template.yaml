test-settings:
  jvm:
    test:
      freeJvmArgs:
        - -XX:+EnableDynamicAgentLoading

  kotlin:
    freeCompilerArgs:
      - -Xjsr305=strict
      - -Xannotation-default-target=param-property

    allOpen:
      presets: [ spring ]

test-dependencies:
  - ../shared/test
