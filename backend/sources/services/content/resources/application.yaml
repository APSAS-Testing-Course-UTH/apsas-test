spring:
  application:
    name: content-service

  config:
    import: "configserver:"

  cloud:
    config:
      discovery:
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
