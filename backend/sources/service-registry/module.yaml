product: jvm/app
apply:
  - ../jvm.module-template.yaml
  - ../spring-cloud.module-template.yaml

settings:
  jvm:
    mainClass: apsas.serviceregistry.ServiceRegistryApplication

dependencies:
  - $libs.spring.cloud.starter.netflix.eureka.server
